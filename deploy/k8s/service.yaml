apiVersion: v1
kind: Service
metadata:
  name: ai-node-app
  namespace: ai-node-app-staging
spec:
  type: ClusterIP
  selector:
    app: ai-node-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
